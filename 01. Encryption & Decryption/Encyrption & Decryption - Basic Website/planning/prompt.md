# My Build prompt
Hello! Today we are going to make an encryption and decryption interface for users. Please do not create new files, use the existing files - index.html, styles.css and scripts.js - and add the code there.

# Layout of the app
The layout fo the app is as follows:
(PS: Refer to the screenshot attcahed name "Website Template.png")
1. The website should have a clean title in H1 that says 
2. Below the H1 , there should be an input box by which users can eneter the data they would like to encyrpt of decrypt
3. Below the input box there should be 2 main sections:
(a) The Key area: This is where the user can enter the key by which they would like to encode or decode their message with
(b) DECODE & ENCODE BUTTONS: Next to the Key area, there should be 2 buttons that are positioned right next to eachother (in a connected format). One Button should say "DECODE" and the other "ENCODE".
4. Below those sections, the output box should be placed by which the user can see their result message of one of the following functions (Encryption & Decryption)

# Functionlaity of the app
This is what the app should do:
1. Allow the user to enter the message they would like to decode or encode in the input box
2. Allow the user to enter the key by which they will encode or decode their data
3. Choose one of the following buttons:
(a)ENCODE: it should turn the data from plain text --> cipher text (unreadable format using the specific key they entered)
(b)DECODE: it should turn the data from cipher text --> plain text (readable format using the specific key they entered)
4. Display the final result fo tehir process in the output box . For this aspect please ensure that the user can see live chnages to their output result data as they keep changing the key.

# Style of the app
The app should have a computer-sciency theme with dark purple and blue tones. It should have a clean interface and a professional look as well.